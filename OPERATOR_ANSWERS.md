# OPERATOR_ANSWERS.md — Section 0 facts

Legend: `CONFIRMED` (real output) · `DISCOVERED` (found read-only in Phase 1) · `MISSING` (blocking).

## ACCESS
- access_method: `local-kubectl`  # CONFIRMED — kubectl runs on control-plane host `mina`
- kubeconfig_path: `/home/mina/.kube/config`  # CONFIRMED
- kube_context_name: `default`  # CONFIRMED — k3s v1.36.2+k3s1, control-plane mina/192.168.1.8
- ssh_target: n/a (agent is local)
- sudo_available: MISSING (worker disk unreadable as a result)
- control_plane_ip: `192.168.1.8`  # CONFIRMED
- worker_node_ips: `10.1.211.122`, `10.1.211.202`  # CONFIRMED (total 3 nodes)

## AGENT-SIDE TOOLCHAIN
- installed_tools: DISCOVERED — kubectl v1.36.2, helm v3.14.0, git 2.53.0, gh (authed Hephast0s),
  docker 29.1.3, trivy 0.72.0, grype 0.116.1, cosign v2.6.4, jq 1.8.1, yq v4.53.3, go 1.24.2,
  node v22.23.1, npm 10.9.8, python3 3.14.4, openssl 3.5.5, curl, wget.
  MISSING: kustomize, syft, gitleaks, semgrep, hadolint, buildkit/kaniko, nerdctl, java, mvn, gradle, dotnet.
- may_install_local_tools: CONFIRMED **yes** (operator, 2026-09-16) — under a **4 GiB total RAM cap** for all tooling/components this engagement adds.
- local_internet_access: DISCOVERED yes (host egress verified)

## CLUSTER EGRESS
- cluster_internet_egress: DISCOVERED `yes` (in-cluster pod test)
- egress_stability: DISCOVERED stable
- registry_pull_allowed: DISCOVERED docker.io, ghcr.io, mcr.microsoft.com, gcr.io, quay.io all reachable
- dockerhub_credentials: MISSING (anonymous; rate-limited)

## PLATFORM COMPONENTS (all DISCOVERED unless noted)
- gitea_url: **ABANDONED** — operator will not run Gitea; source of truth is **GitHub** (2026-09-16)
- git_commit_identity: CONFIRMED `Marshal <Hephast0s@users.noreply.github.com>` (GitHub account Hephast0s)
- jenkins_url: `http://192.168.1.8:30081` — **DOWN (replicas 0)**; api_token MISSING
- jenkins_may_install_plugins / jenkins_k8s_plugin_configured: MISSING (cannot inspect while down)
- harbor_url: `http://harbor.192.168.1.8.nip.io` (core NodePort 30082); v2.15.1 healthy, Trivy on
- harbor_may_create_project: MISSING; harbor_tls: DISCOVERED `http-only`
- nexus_url: DISCOVERED **absent** (not installed); nexus_existing_repos: n/a
- vault_addr: `http://192.168.1.8:30086` (v2.0.3, unsealed); vault_auth_available: MISSING (token needed)
- vault_may_create_mount: MISSING
- gitops_controller: DISCOVERED **argocd v3.4.5** (active) + devtron 2.2.0 present → Argo CD chosen
- gitops_url: `http://192.168.1.8:30085` (argocd NodePort); devtron ingress `devtron.192.168.1.8.nip.io`
- prometheus_stack: DISCOVERED kube-prometheus-stack v0.92.1; grafana_url `http://192.168.1.8:30084`
- loki_present: DISCOVERED **no** (absent); jaeger/otel-collector present instead
- backstage_url: `http://backstage.192.168.1.8.nip.io`; backstage_may_register_location: MISSING

## CLUSTER FACTS
- ingress_class: DISCOVERED `traefik` (default)
- ingress_hostname_pattern: DISCOVERED `<name>.192.168.1.8.nip.io`
- tls_strategy: DISCOVERED `http-only` (no cert-manager/issuer)
- storage_class: DISCOVERED `local-path` (default)
- free_disk_per_node: mina 645 Gi free; workers UNKNOWN (sudo unavailable)
- networkpolicy_enforced: DISCOVERED protocolled `k3s netpol controller enabled` (no disable flag);
  empirical proof in Phase 9

## INSTALL PERMISSIONS (all MISSING — needed before Phases 5/9/11)
- may_install_kyverno: n/a (Kyverno already present, not reinstalled)
- may_install_falco: MISSING
- may_install_eso: n/a (ESO already present)
- may_install_trivy_operator: MISSING
- may_run_zap_jobs: MISSING
- may_install_buildkit_or_kaniko: MISSING
- Loki install (not in Section 0): MISSING decision

## RESOURCE BUDGET (operator directive, 2026-09-16)
- Max total RAM added by this engagement (tool containers + any new cluster components): **4 GiB**.
- Existing platform components are already in the measured baseline; do not exceed the cap without asking.

## SIGNING IDENTITY
- gpg_signing_owner: PARTIAL — CI-bot key generated by the engagement; **operator human key still MISSING**
- gpg_key_id (CI bot): `84DF9F67AAB13638` (ed25519, expires 2028-09-15), pubkey at `security/gpg-ci-bot.pub`
- gpg_key_on_agent_machine: yes (CI-bot private key in `~/.gnupg`; to move to Vault in Phase 5)
- cosign_key_strategy: `generate-new-store-in-vault` (planned, Phase 5)

## PROJECT NAMING
- namespace_prefix: `boutique-` (engagement default, pending confirm; no collision found)
- harbor_project_name: `boutique` (no collision; existing: apps/hephastos/library)
- repo: **`github.com/Hephast0s/boutique-devsecops`** (new, admin) — supersedes Gitea; fork kept as `fork`
- branch model: `main` + `devsecops` (protected) + `phase-<n>-*` work branches + final integrated branch

## PROTOCOL
- approval_phrase: MISSING
- continue_phrase: informal `continue` used
- time_budget_per_phase: MISSING
- pre_engagement_backup: MISSING (Velero 1.18.1 present; operator confirmation required)

---
## Notes
- Corrected: real cluster has **3 nodes** (1 control-plane + 2 workers), not 2.
- Admin (`*.*`) kubeconfig — read-only is a self-imposed discipline; no RBAC backstop.
- **Jenkins and Gitea are scaled to 0** — Phase 2/4 blocked until the operator scales them up (CR-001/002).
